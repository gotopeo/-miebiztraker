# Project TODO

## 基本機能

- [x] 三重県入札情報サイトのスクレイピング機能
- [x] 入札情報のデータベース保存
- [x] LINE通知機能（新着案件の自動通知）
- [x] 管理画面（スクレイピング実行、通知設定）
- [x] Excel出力機能

## スクレイピング機能のPuppeteer移行

### 背景
ChromeDriverエラー「spawn /usr/local/bin/chromedriver-128 ENOENT」を根本的に解決するため、Selenium WebDriverからPuppeteerに完全移行。

### 実装内容
- [x] Selenium WebDriverとChromeDri verの依存関係を削除
- [x] Puppeteerをインストール
- [x] server/scraper.tsをPuppeteerベースに書き換え
- [x] convertToInsertBidding関数とscrapeMieBiddings関数を復元
- [x] ローカルテストで10件の入札情報を正常に取得することを確認

## Puppeteerブラウザインストール問題の解決

### フェーズ1: 問題の特定
- [x] 本番環境でのスクレイピング失敗を確認
- [x] エラーメッセージを確認（Could not find Chrome (ver. 145.0.7632.46)）
- [x] Puppeteerのブラウザが本番環境にインストールされていないことを特定

### フェーズ2: 解決策の実装
- [x] package.jsonにpostinstallスクリプトを追加してChromiumを自動インストール
- [x] Puppeteerの設定を修正（executablePathの指定）
- [x] .puppeteerrc.cjsを作成してキャッシュパスを設定
- [x] プロジェクト内にChromiumをインストール
- [x] サーバー再起動

### フェーズ3: 動作確認とチェックポイント保存
- [x] 本番環境で手動スクレイピングを実行
- [x] スクレイピングが成功することを確認
- [x] チェックポイントを保存

---

## 本番環境でのPuppeteer executablePath問題の解決

### 問題
本番環境では `/usr/src/app/.cache/puppeteer/chrome/linux-145.0.7632.46/chrome-linux64/chrome` を参照しているが、Chromiumが見つからない。

### フェーズ1: 問題の特定
- [x] 本番環境でのエラーメッセージを確認
- [x] 本番環境のワーキングディレクトリが `/usr/src/app` であることを特定

### フェーズ2: 解決策の実装
- [x] .puppeteerrc.cjsを修正して本番環境のパスに対応
- [x] package.jsonのpostinstallスクリプトを確認
- [x] サーバー再起動

### フェーズ3: 動作確認とチェックポイント保存
- [x] チェックポイントを保存
- [ ] デプロイ後、本番環境で手動スクレイピングを実行
- [ ] スクレイピングが成功することを確認

---

## postinstallスクリプトの根本的な修正（専門家の指摘に基づく）

### 問題の本質
現在の`postinstall`スクリプトは`puppeteer.executablePath()`が「実在チェック」ではなく「パス文字列を返すだけ」であるため、Chromiumが存在しなくてもcatchに落ちず、インストールが実行されない。

### フェーズ1: タスク追加
- [x] todo.mdに新しいタスクを追加

### フェーズ2: 実在チェック付きpostinstallスクリプトを作成
- [x] `scripts/ensure-chrome.mjs`を作成（fs.existsSyncで実在チェック）
- [x] package.jsonのpostinstallを修正

### フェーズ3: scraper.tsに起動時Chromiumチェックを追加
- [x] `ensureChrome()`関数を追加（実行時に自己修復）
- [x] `initBrowser()`の冒頭で`ensureChrome()`を呼び出し

### フェーズ4: 環境情報ログを追加
- [x] cwd、uid、PUPPETEER_CACHE_DIR、executablePath、existsをログ出力（ensureChrome関数内に実装）
- [x] キャッシュディレクトリの内容を確認するログを追加（必要に応じて追加可能）

### フェーズ5: 動作確認とチェックポイント保存
- [x] 開発環境でスクレイピングを実行（新しい実行履歴が表示されない）
- [ ] サーバーログを確認してensureChrome関数のログを確認
- [ ] チェックポイントを保存
- [ ] Publish後、本番環境でスクレイピングを実行
- [ ] 本番環境のログを確認して問題を特定

---

## Chromium実行に必要なシステムライブラリのインストール

### 問題の本質
Chromiumのバイナリはインストールされたが、実行に必要なシステムライブラリ（libglib-2.0.so.0など）が不足している。

### エラー内容
```
Failed to launch the browser process: Code: 127
stderr: /usr/src/app/.cache/puppeteer/chrome/linux-145.0.7632.46/chrome-linux64/chrome: error while loading shared libraries: libglib-2.0.so.0: cannot open shared object file: No such file or directory
```

### フェーズ1: タスク追加
- [x] todo.mdに新しいタスクを追加

### フェーズ2: システムライブラリインストールスクリプトを作成
- [x] `scripts/ensure-chrome.mjs`を修正して`--install-deps`フラグを使用
- [x] `server/scraper.ts`の`ensureChrome()`も同様に修正
- [x] root権限がない場合のフォールバック処理を追加

### フェーズ3: 動作確認とチェックポイント保存
- [x] チェックポイントを保存
- [ ] Publish後、本番環境でスクレイピングを実行
- [ ] システムライブラリが正常にインストールされ、スクレイピングが成功することを確認


---

## --install-depsが機能しない問題の原因特定と代替手段

### 状況
Publish後も同じエラーが発生：`libglib-2.0.so.0: cannot open shared object file`
`--install-deps`フラグが機能していない可能性が高い。

### 推測される原因
1. Manus本番環境でroot権限がなく、システムライブラリのインストールが失敗している
2. `@puppeteer/browsers`パッケージの`--install-deps`がManusのコンテナ環境で動作しない
3. postinstallスクリプトが本番環境で実行されていない、またはエラーを無視している

### フェーズ1: タスク追加
- [x] todo.mdに新しいタスクを追加

### フェーズ2: デプロイログを確認して原因を特定
- [ ] デプロイログ（ビルドログ）を確認してpostinstallが実行されたか確認
- [ ] `--install-deps`が成功したか、フォールバックしたかを確認
- [ ] エラーメッセージがあれば記録

### フェーズ3: 代替手段を実装
- [ ] オプション1: Playwrightに移行（システムライブラリが含まれたブラウザバイナリ）
- [ ] オプション2: puppeteer-coreとchrome-aws-lambdaを使用（AWS Lambda用だがコンテナ環境でも動作する可能性）
- [ ] オプション3: Dockerfileを作成してシステムライブラリを事前インストール（Manusがサポートしている場合）
- [ ] オプション4: 外部スクレイピングAPIサービスを使用（ScrapingBee、Apifyなど）

### フェーズ4: 動作確認とチェックポイント保存
- [ ] 選択した代替手段を実装
- [ ] 開発環境でテスト
- [ ] チェックポイントを保存
- [ ] Publish後、本番環境でスクレイピングが成功することを確認


---

## PuppeteerからPlaywrightへの移行

### 移行理由
- Puppeteerはシステムライブラリ（libglib-2.0.so.0など）への依存が強く、Manus本番環境でインストールできない
- Playwrightは自己完結型ブラウザバイナリを提供し、コンテナ環境に最適化されている
- PuppeteerとAPIが類似しているため、移行が比較的容易

### フェーズ1: タスク追加
- [x] todo.mdに新しいタスクを追加

### フェーズ2: Playwrightをインストールし、Puppeteerを削除
- [x] `pnpm add playwright`を実行
- [x] `pnpm remove puppeteer`を実行

### フェーズ3: scraper.tsをPlaywright APIに書き換え
- [x] `import puppeteer from 'puppeteer'` → `import { chromium } from 'playwright'`
- [x] `puppeteer.launch()` → `chromium.launch()`
- [x] `page.setViewport()` → `page.setViewportSize()`
- [x] `waitUntil: 'networkidle2'` → `waitUntil: 'networkidle'`
- [x] `browser.once('targetcreated')` → `page.context().waitForEvent('page')`
- [x] ensureChrome関数を削除（Playwrightは自動インストール）

### フェーズ4: postinstallスクリプトをPlaywright用に修正
- [x] package.jsonのpostinstallを`npx playwright install chromium --with-deps`に変更
- [x] 開発環境でChromiumをインストール

### フェーズ5: 開発環境でテスト
- [x] サーバーを再起動
- [x] ローカルテストスクリプトでPlaywrightが正常に動作することを確認

### フェーズ6: チェックポイント保存と本番環境での確認
- [x] チェックポイントを保存（d20d4879）
- [ ] Publish後、本番環境でスクレイピングを実行
- [ ] Playwrightが正常に動作し、スクレイピングが成功することを確認

---

## デプロイエラー修正（apt-get: not found）

### 問題
- `npx playwright install chromium --with-deps`が本番環境で失敗
- `apt-get: not found` - パッケージマネージャーが利用できない
- `--with-deps`フラグがシステムライブラリのインストールを試みるが、権限がない

### 解決策
- `--with-deps`フラグを削除して、Playwrightのブラウザバイナリのみをインストール
- Playwrightのブラウザバイナリは自己完結型なので、システムライブラリなしでも動作するはず

### フェーズ1: todo.mdにタスクを追加
- [x] 問題を記録

### フェーズ2: postinstallスクリプトから--with-depsを削除
- [x] package.jsonのpostinstallを`npx playwright install chromium`に変更

### フェーズ3: チェックポイント保存とデプロイ
- [x] チェックポイントを保存
- [ ] Publish後、デプロイが成功することを確認
- [ ] 本番環境でスクレイピングを実行して動作確認


---

## スケジュール実行成功・手動実行失敗の原因調査

**重要な発見**: 2/12 16:00のスケジュール実行で100件のスクレイピングが成功している！
これは、Playwrightが本番環境で動作していることを意味する。

### フェーズ1: todo.mdにタスクを追加
- [x] タスクを追加

### フェーズ2: スケジュール実行と手動実行の違いを調査
- [ ] スクレイピングログを確認して成功した実行の詳細を確認
- [ ] 手動実行とスケジュール実行のコード実行パスの違いを確認
- [ ] タイムアウト設定の違いを確認
- [ ] 環境変数の違いを確認

### フェーズ3: 手動実行のエラー原因を特定して修正
- [x] 手動実行のエラーログを詳細に分析（tRPCタイムアウトが原因）
- [x] HTTPサーバーのタイムアウトを5分に延長
- [x] tRPC手動スクレイピングのmetaにタイムアウト設定を追加

### フェーズ4: 100件制限を171件に拡張
- [x] スクレイピング件数の制限箇所を特定（scraper.ts 417行目）
- [x] ページネーション制限を10ページから20ページに拡張
- [x] 171件すべてを取得できるように修正

### フェーズ5: テストとチェックポイント保存
- [x] チェックポイントを保存
- [ ] Publish後、手動実行が成功することを確認
- [ ] 171件すべてが取得できることを確認


---

## 手動実行でlibglib-2.0.so.0エラーが発生

**現状**: 
- 手動実行: `libglib-2.0.so.0: cannot open shared object file`エラーで失敗
- スケジュール実行: ユーザー報告では2/12 16:00に100件成功

**目標**: スケジュール実行の成功状況を確認し、手動実行でも動作するようにする

### フェーズ1: todo.mdにタスクを追加
- [x] 新しい調査タスクを追加

### フェーズ2: データベースを確認してスケジュール実行の成功状況を特定
- [ ] 2/12 16:00前後のデータを確認
- [ ] 実際に新しいデータが保存されているか確認
- [ ] スクレイピング実行履歴テーブルを確認

### フェーズ3: スケジュール実行と手動実行の環境差異を調査
- [ ] 実行ユーザーの違いを確認
- [ ] 環境変数の違いを確認
- [ ] プロセスの違いを確認

### フェーズ4: 解決策を実装または代替手段を提案
- [ ] 解決策を実装
- [ ] または外部スクレイピングサービスの導入を提案


---

## スケジュール実行成功・手動実行失敗の差分調査（完了）

**調査結果**:
- スケジュール実行は**古いデプロイ（Puppeteer時代）のコード**で動作している
- 手動実行は**最新のデプロイ（Playwright）のコード**で動作している
- Playwrightのpostinstallで`--with-deps`が失敗し、システムライブラリがインストールされていない
- 手動実行は本番環境で`libglib-2.0.so.0`エラーで失敗している

**結論**: Puppeteerに戻すことで問題を最も確実に解決できる

### フェーズ1: 調査完了
- [x] スケジュール実行と手動実行のコードパスを比較
- [x] エラーログを詳細に分析
- [x] 根本原因を特定（デプロイ時期の違い）
- [x] 調査結果をドキュメント化

### フェーズ2: Puppeteerに戻す
- [x] PlaywrightをアンインストールしてPuppeteerをインストール
- [x] server/scraper.tsをPuppeteer APIに書き換え
- [x] postinstallスクリプトを修正
- [x] .puppeteerrc.cjsを復元

### フェーズ3: テストとチェックポイント保存
- [ ] 開発環境でテスト
- [ ] チェックポイントを保存
- [ ] Publish後、手動実行が成功することを確認
- [ ] 新規通知設定が動作することを確認


---

## 新しいスケジュールが0件になる問題の調査

**状況**:
- 過去に成功していたスケジュール：Selenium時代に設定（230件取得成功）
- 新しく設定したスケジュール：Puppeteerで実行、成功と表示されるが0件
- 手動スクレイピング：まだテストしていない

### フェーズ1: 新しいスケジュール実行のログとコードを詳細に調査
- [ ] 新しいスケジュール実行のログを確認
- [ ] スケジュール実行のコードパスを確認
- [ ] 検索条件の違いを確認
- [ ] スクレイピング結果の詳細を確認

### フェーズ2: 0件になる原因を特定し、修正案を実装
- [x] 原因を特定（ページ遷移の待機が不十分）
- [x] 修正案を実装（waitForNavigationを使用）

### フェーズ3: 修正をテストしてチェックポイント保存
- [ ] 開発環境でテスト
- [ ] チェックポイントを保存
- [ ] Publish後、本番環境でテスト


---

## PuppeteerからSeleniumへの移行（本番環境でのlibglib-2.0.so.0エラー解決）

**状況**:
- 手動スクレイピング: libglib-2.0.so.0エラーで失敗
- スケジュール実行: 成功と表示されるが0件
- 過去のスケジュール（Selenium時代）: 230件取得成功

**根本原因**:
- Manus本番環境では、Puppeteerのpostinstallスクリプトでシステムライブラリをインストールできない

**解決策**:
- 過去に成功していたSeleniumに戻す

### フェーズ1: SeleniumパッケージをインストールしてPuppeteerをアンインストール
- [x] Puppeteerをアンインストール
- [x] Seleniumパッケージをインストール
- [x] Chromedriverをインストール

### フェーズ2: scraper.tsをSelenium APIに書き換え
- [x] import文をSeleniumに変更
- [x] initBrowser関数をSelenium APIに変更
- [x] navigateFromTopPage関数をSelenium APIに変更
- [x] executeSearch関数をSelenium APIに変更
- [x] その他の関数をSelenium APIに変更
- [x] convertToInsertBidding関数を追加
- [x] SearchConditions型に不足していたフィールドを追加

### フェーズ3: チェックポイントを保存してデプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ（ユーザーが実行）

### フェーズ4: 本番環境でテストして結果を報告
- [ ] 手動スクレイピングをテスト
- [ ] スケジュール実行を確認
- [ ] 結果を報告


---

## デプロイエラーの修正（ensure-chrome.mjsがPuppeteerをimport）

**エラー内容**:
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'puppeteer' imported from /tmp/deploy-s3-v2-2494819927/scripts/ensure-chrome.mjs
```

**原因**:
- `scripts/ensure-chrome.mjs`がまだPuppeteerをimportしようとしている
- postinstallスクリプトで`node scripts/ensure-chrome.mjs`が実行される

**解決策**:
- `scripts/ensure-chrome.mjs`をSelenium用に修正
- または、postinstallスクリプトを削除（Seleniumのchromedriverは自動でインストールされる）

### フェーズ1: ensure-chrome.mjsをSelenium用に修正
- [x] ensure-chrome.mjsの内容を確認
- [x] postinstallスクリプトを削除（Seleniumのchromedriverが自動インストール）

### フェーズ2: チェックポイントを保存して再デプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ（ユーザーが実行）

### フェーズ3: 本番環境でテストして結果を報告
- [ ] 手動スクレイピングをテスト
- [ ] スケジュール実行を確認


---

## スケジュールが連続実行される問題の調査

**現象**:
- 2026/2/15 21:24:29〜21:24:33の間に20件以上のスケジュール実行が連続発生
- すべて「成功」だが、取得件数0件、新規件数0件
- 2日ぶりにAIを起動したら発生

**仮説**:
1. 過去に実行されなかったスケジュールが一気に実行された（キャッチアップ動作）
2. スケジュール設定が重複している
3. スケジューラーのロジックに問題がある

### フェーズ1: スケジュール設定とスケジューラーのコードを調査
- [ ] データベースのnotification_schedulesテーブルを確認
- [ ] scheduler.tsのスケジュール登録ロジックを確認
- [ ] node-scheduleのキャッチアップ動作を確認

### フェーズ2: 連続実行の原因を特定し、修正案を実装
- [ ] 原因を特定
- [ ] 修正案を実装

### フェーズ3: 修正をテストしてチェックポイント保存
- [ ] 修正をテスト
- [ ] チェックポイントを保存

### フェーズ4: 調査結果と修正内容をユーザーに報告
- [ ] 調査結果をまとめる
- [ ] 本番環境での不都合を説明


---

## Selenium版デプロイ後の確認

### フェーズ1: 本番環境のデータベースから最新のスクレイピングログを確認
- [ ] scrapingLogsテーブルから最新のログを取得
- [ ] スケジュール実行（scheduled）のログを確認
- [ ] 手動実行（manual）のログを確認

### フェーズ2: スケジュール実行の成否を分析
- [ ] 成功したスケジュール実行の件数を確認
- [ ] 失敗したスケジュール実行の件数とエラー内容を確認
- [ ] 取得件数と新規件数を確認

### フェーズ3: 問題があれば原因を特定して修正
- [x] エラーがあれば原因を特定（ChromeDriverのバージョン不一致）
- [x] 修正案を実装（ChromeDriverを128にダウングレード）

### フェーズ4: 確認結果をユーザーに報告
- [x] 確認結果をまとめる
- [x] 本番環境での動作状況を報告


---

## 本番環境でのService Unavailableエラーの解決

### 問題
手動スクレイピング実行時に「Unexpected token 'S', "Service Unavailable" is not valid JSON」エラーが発生。

### フェーズ1: 本番環境のログとエラー状況を調査
- [x] 本番環境のスクレイピングログを確認
- [x] サーバーログを確認
- [x] エラーの詳細を特定（TimeoutError: 存在しないリンクを探している）

### フェーズ2: Service Unavailableエラーの原因を特定
- [x] タイムアウトの可能性を確認
- [x] 三重県サイトの構造変更を確認
- [x] 原因特定：「入札情報サービスシステム（公共調達）」リンクが存在しない

### フェーズ3: 修正案を実装してテスト
- [x] 原因に応じた修正を実装（画像リンクをクリックするように変更）
- [x] ローカル環境でテスト
- [x] チェックポイントを保存

### フェーズ4: 修正内容をユーザーに報告
- [ ] 修正内容をまとめる
- [ ] 本番環境での動作確認手順を案内


---

## 手動スクレイピングが「実行中」のまま完了しない問題

### フェーズ1: 本番環境のログを確認して停止箇所を特定
- [x] 本番環境のサーバーログを確認
- [x] どの処理で停止しているか特定（ElementNotInteractableError）
- [x] タイムアウト設定を確認

### フェーズ2: タイムアウトまたは無限ループの原因を分析
- [x] Seleniumの待機処理を確認
- [x] ページ遷移の処理を確認
- [x] 原因特定：要素のサイズがゼロでクリックできない

### フェーズ3: 修正案を実装してテスト
- [x] 原因に応じた修正を実装（JavaScriptで強制クリック、待機時間延長）
- [x] ローカル環境でテスト
- [x] チェックポイントを保存

### フェーズ4: 修正内容をユーザーに報告
- [ ] 修正内容をまとめる
- [ ] 本番環境での動作確認手順を案内


---

## 本番環境で手動スクレイピングがまだ実行中の問題（2回目の報告）

### フェーズ1: 本番環境の最新ログとステータスを確認
- [x] 本番環境のスクレイピングログを確認
- [x] サーバーログでエラーを確認
- [x] タイムアウト設定を確認（10件の「running」ステータスを発見）
- [x] 古い「running」ステータスを「failed」に更新

### フェーズ2: 開発環境でスクレイピングをテストして動作確認
- [x] タイムアウト処理が実装されていないことを発見
- [x] scrapeMieBiddings関数に5分のタイムアウトを追加

### フェーズ3: 問題があれば修正して再デプロイ
- [x] 原因に応じた修正を実装（5分のタイムアウトを追加）
- [x] チェックポイントを保存
- [ ] デプロイ（ユーザーが実行）

### フェーズ4: 結果をユーザーに報告
- [ ] 修正内容をまとめる
- [ ] 本番環境での動作確認手順を案内


---

## スクレイピングのタイムアウトを1分に短縮

### 問題
手動スクレイピングが「実行中」のまま終わらない。ユーザーは1分以内に完了しない場合は「失敗」と表示してほしい。

### フェーズ1: タイムアウト時間を5分から1分に短縮
- [x] scraper.tsのscrapeMieBiddings関数のタイムアウトだ60秒に変更
- [x] routers.tsのtRPCタイムアウトだ60秒に変更
- [x] server/_core/index.tsのHTTPサーバータイムアウトを確認（必要に応じて調整）

### フェーズ2: チェックポイント保存とデプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ（ユーザーが実行）

### フェーズ3: 本番環境でテストして結果を報告
- [ ] 手動スクレイピングをテスト
- [ ] 1分以内に完了しない場合に「失敗」と表示されることを確認


---

## 詳細検索条件機能を削除して最新公告情報のみに絞り込み

### 背景
詳細検索条件の絞り込みは通知設定で行うため、スクレイピング時の詳細検索は不要。常に「最新公告情報」を取得することで、スクレイピング処理をシンプルにし、成功率を向上させる。

### フェーズ1: 詳細検索機能を削除
- [x] scraper.tsのsetSearchConditions関数を削除
- [x] SearchConditions型から詳細検索用のフィールドを削除（useLatestAnnouncementのみ残す）
- [x] scrape関数から詳細検索の条件分岐を削除
- [x] executeSearch関数を常に「最新公告情報」ボタンをクリックするように修正

### フェーズ2: routers.tsの詳細検索エンドポイントを削除
- [x] executeWithConditionsエンドポイントを削除
- [x] executeエンドポイントを簡素化

### フェーズ3: フロントエンドの詳細検索UIを削除（必要に応じて）
- [ ] 管理画面の詳細検索フォームを削除または非表示に

### フェーズ4: チェックポイント保存とデプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ

### フェーズ5: 本番環境でテスト
- [ ] 手動スクレイピングが成功することを確認
- [ ] 1分以内に完了することを確認


---

## データベース上の古い「実行中」ステータスをクリーンアップ

### 問題
スクレイピング履歴に多数の「実行中」ステータスが残っている。これは過去のスクレイピングプロセスが異常終了したため、データベース上のステータスが「実行中」のまま残っている状態。

### フェーズ1: データベースの「実行中」レコードを特定して「失敗」に更新
- [ ] scrapingLogsテーブルのstatus='running'のレコードを特定
- [ ] これらのレコードのstatusを'failed'に更新
- [ ] errorMessageに「Process terminated by server restart」を設定
- [ ] finishedAtに現在時刻を設定

### フェーズ2: 更新結果を確認してユーザーに報告
- [ ] 更新されたレコード数を確認
- [ ] ユーザーに報告


---

## 「Service Unavailable」エラーの原因を特定して修正

### 問題
手動スクレイピングを実行すると「Service Unavailable」エラーが発生する。これは、本番環境のサーバーがスクレイピング処理中にタイムアウトまたはクラッシュしていることを示している。

### 考えられる原因
1. Seleniumのブラウザ起動に失敗している
2. ChromeDriverのバージョン不一致
3. 本番環境でのメモリ不足
4. LinkSubmit関数が見つからない（JavaScriptエラー）

### フェーズ1: エラーログを確認して原因を特定
- [ ] 本番環境のサーバーログを確認
- [ ] scrapingLogsテーブルの最新エラーメッセージを確認
- [ ] 開発環境でスクレイピングをテストして動作確認

### フェーズ2: Seleniumのブラウザ起動問題を修正
- [ ] ChromeDriverのバージョンを確認
- [ ] ブラウザオプションを見直し（headlessモード、メモリ設定など）
- [ ] エラーハンドリングを強化

### フェーズ3: チェックポイントを保存してデプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ
- [ ] 本番環境でテスト


---

## LinkSubmit関数エラーの修正

### 問題
開発環境でのテスト結果、`LinkSubmit is not defined`エラーが発生。検索ページまでは到達しているが、「最新公告情報」ボタンをクリックする際にJavaScript関数が見つからない。

### フェーズ1: executeSearch関数を修正してボタン要素を直接クリック
- [x] executeSearch関数でLinkSubmit関数の代わりにボタン要素を探す
- [x] ボタン要素を直接クリックする処理に変更（実際はリンク要素だった）
- [x] 待機時間を調整

### フェーズ2: 開発環境で再テストして成功を確認
- [ ] 開発環境でスクレイピングを実行
- [ ] データが正常に取得できることを確認
- [ ] 1分以内に完了することを確認

### フェーズ3: チェックポイントを保存してデプロイ
- [x] チェックポイントを保存
- [ ] Publishボタンでデプロイ
- [ ] 本番環境でテスト


---

## 手動スクレイピングを非同期処理に変更

### 問題
手動スクレイピングがHTTPタイムアウト（通常30秒〜60秒）により「Service Unavailable」エラーが発生。一方、スケジュール実行は成功している。

### 原因
- 手動実行：ブラウザ → サーバー → スクレイピング処理（HTTPリクエストのタイムアウトの影響を受ける）
- スケジュール実行：サーバー内部で直接実行（タイムアウトの影響を受けない）

### フェーズ1: 手動スクレイピングのエンドポイントを非同期処理に変更
- [x] routers.tsのexecuteエンドポイントを非同期処理に変更
- [x] スクレイピングをバックグラウンドで実行するように修正
- [x] すぐに「スクレイピングを開始しました」というレスポンスを返す

### フェーズ2: フロントエンドを修正してポーリングで結果を確認
- [x] スクレイピング実行後、実行履歴をポーリングで更新（5秒ごとに10回）
- [x] 実行中のステータスを表示（実行履歴テーブルに表示）
- [x] 完了後、結果を自動的に表示（ポーリングにより自動更新）

### フェーズ3: チェックポイントを保存してデプロイ
- [ ] チェックポイントを保存
- [ ] Publishボタンでデプロイ
- [ ] 本番環境でテスト
